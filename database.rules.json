{
  "rules": {
    ".read": true,
    "admins": {
      ".write": "auth != null && root.child('admins').child(auth.uid).val() === true",
      "$uid": { ".validate": "newData.isBoolean()" }
    },
    "players": {
      ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
    },
    "items": {
      ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
    },
    "checks": {
      ".write": "auth != null"
    }
  }
}
